server {
    listen 80;
    server_name _;

    # Static files
    root /usr/share/nginx/html;
    index index.html;

    # Auth subrequest endpoint
    location = /_auth {
        internal;
        proxy_pass         http://reader-api:8000/api/auth/verify;
        proxy_pass_request_body off;
        proxy_set_header   Content-Length "";
        proxy_set_header   X-Original-URI $request_uri;
    }

    # API — proxy to FastAPI
    location /api/ {
        proxy_pass         http://reader-api:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        # Large uploads (books can be big)
        client_max_body_size 200M;
        proxy_read_timeout   300s;
    }

    # Login page — no auth check
    location = /login.html {
        try_files $uri =404;
    }

    # Everything else — require auth cookie (checked by FastAPI /api/auth/verify)
    location / {
        auth_request /_auth;
        error_page 401 = @login_redirect;
        try_files $uri $uri/ /index.html;
    }

    location @login_redirect {
        return 302 /login.html?next=$request_uri;
    }
}
